import styled from 'styled-components';

const A = styled.a`
  color: inherit;
  text-decoration: none;
  &:link, &:visited, &:link:active, &:visited:active {
    color: inherit;
  }
`;

export default A;

