{
  "name": "hsl-shared-components",
  "version": "0.9.7",
  "description": "Shared components to be used in HSL projects. React and styled components.",
  "devDependencies": {
    "lerna": "^2.0.0"
  },
  "scripts": {
    "test": "lerna run test",
    "test-update": "lerna run test-update",
    "es-lint": "lerna run es-lint"
  }
}
